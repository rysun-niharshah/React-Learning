/**
 * Global state (Redux/Zustand) — use when Context is insufficient (normalized
 * server cache, middleware, time-travel debugging). Feature slices live in
 * store/slices/; configureStore in store/index.ts.
 *
 * This template uses Context for auth; add Redux here when you need it:
 *
 *   store/
 *     index.ts          — configureStore, typed hooks
 *     slices/
 *       authSlice.ts
 *       ordersSlice.ts
 */
export {};
