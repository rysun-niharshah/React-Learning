/**
 * assets/ — images, fonts, and SVGs imported into components (processed by Vite).
 * Use public/ for files that must keep their exact filename/URL (favicon, manifest).
 * Use assets/ for everything bundled and cache-busted by the build.
 */
export { default as reactLogo } from "./react.svg";
