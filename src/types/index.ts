/**
 * Barrel re-export — optional convenience file. Import directly from domain files
 * (types/user) in new code; barrels help when many types are needed at once.
 */
export type { User } from "./user";
export type { DashboardStat } from "./dashboard";
